apiVersion: v1
kind: Secret
metadata:
  name: minio
  labels:
    app.kubernetes.io/name: minio
    app.kubernetes.io/component: object-storage
type: Opaque
stringData:
  MINIO_ROOT_USER: "harness_minio"
  MINIO_ROOT_PASSWORD: "harness_development_password"
