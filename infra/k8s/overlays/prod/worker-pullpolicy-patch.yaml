apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-3d
spec:
  template:
    spec:
      containers:
        - name: worker-3d
          # Ảnh được nạp sẵn vào node bằng `k3d image import`; không kéo từ registry.
          imagePullPolicy: IfNotPresent
